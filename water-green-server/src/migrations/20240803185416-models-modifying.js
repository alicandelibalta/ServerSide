'use strict';

module.exports = {
  up: async (queryInterface, Sequelize) => {
    // Yapılacak değişiklikler
  },

  down: async (queryInterface, Sequelize) => {
    // Geri alınacak değişiklikler
  }
};
